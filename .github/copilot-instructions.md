# Copilot Instructions

Canonical agent instructions for this repository are in `AGENTS.md` (repo
root). If your Copilot surface has not already loaded that file, read it
before making changes — it covers the repository layout, the mandatory
`dotfiles-link` workflow, and pointers to the path-scoped style guides in
`.github/instructions/`.
