= TODO: Shell Script Improvements
:toc:

Ideas and future tasks for improving the shell scripts in this repository.

== 1. Repo Structure & Organization

=== Script Consolidation

* [x] Integrate `Useful-Scripts/` into the main repo structure (from https://github.com/cbaoth/Useful-Scripts)
** Reviewed each script for relevance and quality
** Moved system/root scripts to `system-scripts/`
** Moved user utility scripts to `bin/`
** Archived outdated/deprecated scripts to `_archive/` (see <<archived-scripts-review>>)
** Merged `Useful-Scripts/README.adoc` into the main `README.adoc`
* [ ] Audit `bin/` scripts against the style guide
** Standardize shebangs (`#!/usr/bin/env bash`)
** Add missing file headers
** Update `set` options and error handling
** Apply naming conventions
** NOTE: Includes newly added scripts from Useful-Scripts integration (apt-update, exif-move-to-rating-dirs, smfetch)
* [ ] Review `.zsh.d/` files for consistency
** Include a review for shell-agnostic content that could be moved/shared so it
   can be reused from bash-only setups.
* [ ] Consolidate duplicate logging/output implementations across scripts
  (prefer `commons.sh` where appropriate)
* [ ] Review and resolve FIXME/TODO comments in `system-scripts/dbbackup`

=== Directory Structure

* [~] `~/bin/` vs. `~/.local/bin/`: Decision made to keep `~/bin/` for now.
  XDG Base Directory Specification suggests `~/.local/bin/`, but `~/bin/` is
  conventional and most distributions add it to `PATH` automatically.
  Revisit if/when a full XDG migration is planned.
* [ ] Consider organizing scripts by category if the collection grows
* [x] Move actual dotfiles into `dotfiles/` subdirectory, and repo tooling
  (`link.sh`, `.linkignore`, `fix-modelines.py`, etc.) into `tools/`.
** `tools/link.sh` updated to scan `$REPO_ROOT/dotfiles/`
** `tools/.linkignore` simplified (non-dotfile files are no longer in the scan path)
* [ ] Review the dotfile linking approach — the current `link.sh` + `_overrides/` solution
  is a rough first cut. Specifically:
** Define and document a portability contract for this repo:
  no root requirement, no package-install requirement, and usable baseline on
  bash-only hosts.
** Keep `tools/link.sh` dependency-free (pure shell tooling). Treat GNU Stow /
  chezmoi as optional alternatives, not hard requirements.
** Add a design note for optional capability detection (bash/zsh/tools) and
  profile-based setup decisions, but do not increase script complexity yet.
** `_overrides/` was a quick solution for host-specific file overrides; reconsider
   how host-specific overrides should work (e.g. host-specific branches, chezmoi templates, or a clean override mechanism in link.sh).
** The underscore-prefix convention for "non-repo" dirs (`_local`, `_archive`, `_overrides`)
   is useful but may conflict if `_*` is ever added to `.gitignore`. Consider whether a
   dedicated `.nolink` file (gitignore-style) would be cleaner than `.linkignore`.
** Evaluate alternatives to the current find+symlink approach: https://www.gnu.org/software/stow/[GNU Stow],
   https://www.chezmoi.io/[chezmoi], or a significantly cleaned-up `link.sh`.
** Evaluate a shell-agnostic dynamic loader for host/OS specific aliases,
   functions and rc fragments, so shared logic can be sourced from both zsh and
   bash where possible.

== 2. Code Quality & Style

=== Linting & Static Analysis

* [ ] Integrate https://www.shellcheck.net/[ShellCheck] into the development
  workflow
** VS Code extension:
    https://marketplace.visualstudio.com/items?itemName=timonwong.shellcheck[ShellCheck for VS Code]
** Run against all scripts: `find bin/ lib/ system-scripts/ -type f -exec shellcheck {} +`
** Consider a configuration file (`.shellcheckrc`) for project-wide settings
* [ ] Evaluate other linting tools (e.g.,
  https://github.com/mvdan/sh[shfmt] for formatting)

=== Pre-Commit Hooks

* [ ] Investigate https://pre-commit.com/[pre-commit] framework
* [ ] Add ShellCheck as a pre-commit hook
* [ ] Add shfmt formatting check as a pre-commit hook
* [ ] Consider a simple custom hook that validates shebangs and file headers

=== Testing

* [ ] Evaluate shell testing frameworks:
** https://github.com/bats-core/bats-core[BATS] (Bash Automated Testing
     System)
** https://github.com/shellspec/shellspec[ShellSpec]
* [ ] Write tests for `lib/commons.sh` utility functions
* [ ] Consider testing critical `bin/` scripts (argument parsing, edge cases)

=== Consistency & Maintenance Audit

* [ ] Run a repo-wide audit for redundant/outdated/broken/no-longer-used
  scripts, functions, files and features
* [ ] Build an inventory of repeated or diverging implementations (logging,
  output formatting, argument parsing, capability checks, etc.)
* [ ] Define consolidation targets and migrate callers to shared helpers where
  sensible (prefer `lib/commons.sh` or a small shared loader)
* [ ] Establish a ShellCheck cleanup baseline and iteratively reduce warnings to
  minimal/near-zero for active scripts

== 3. Improvements & Ideas for Specific Scripts

=== commons.sh Improvements

* [ ] Review and clean up `commons.sh`
** Fix known typos in comments (e.g., `FUNCTONS` -> `FUNCTIONS`)
** Ensure all functions have proper documentation comments
** Add missing type declarations (`-r`, `-i`, `-a`)
* [ ] Expand test coverage for commons.sh functions
* [ ] Consider versioning commons.sh for backward compatibility tracking
* [ ] Streamline the sourcing mechanism for commons.sh across scripts. Example:
. Keep one stable loader file (known path).
. Loader finds and sources commons.sh, validates required symbols:
+
[source,bash]
----
# ~/lib/commons-loader.sh: Locate and source commons.sh once
# code: language=bash insertSpaces=true tabSize=2
# -*- mode: sh; sh-shell: bash; indent-tabs-mode: nil; tab-width: 2 -*-
# vim: ft=bash:et:ts=2:sts=2:sw=2
# keywords: bash shell loader commons
# author: Andreas Weyer

cl::require_commons() {
  local -a candidates=(
    "${SCRIPT_PATH}/commons.sh"
    "${SCRIPT_PATH}/lib/commons.sh"
    "${HOME}/lib/commons.sh"
  )
  local f

  for f in "${candidates[@]}"; do
    if [[ -f "${f}" ]]; then
      # shellcheck source=/dev/null
      source "${f}"
      break
    fi
  done

  if ! command -v "cl::cmd_p" > /dev/null 2>&1; then
    printf "commons lib not found, exiting ..\n" >&2
    return 1
  fi

  return 0
}
----
. Scripts use a 1-liner source + 1-liner cl::require_commons call:
+
[source,bash]
----
# ...existing code...
source "${HOME}/lib/commons-loader.sh" || exit 1
cl::require_commons || exit 1
# ...existing code...
----

=== Review .vimrc and Sync with new Modeline

* [ ] Review local Vim settings in repo `.vimrc` to confirm modeline options match local config
** Verify `expandtab tabstop=2 shiftwidth=2` and `filetype=bash/bash/sh` align with `.vimrc` defaults
** (Modeline will override vimrc per-file; this is a post-hoc consistency check)

== 4. General Improvements & Ideas

=== Text formatting

* [ ] Consolidate output text formatting (if needed)
** [ ] Consider https://zsh.sourceforge.io/Doc/Release/Prompt-Expansion.html[Zsh Prompt Expansion] for zsh scripts (e.g. `print -P "%Uunderlined%u regular"`)


== A.1 Editor Modelines ✅

* [x] Review existing VS Code modelines across relevant files and file types
** All `# code: language=(bash|zsh)` modelines are consistent across `bin/`, `lib/`, `Useful-Scripts/`
* [x] Check for additional relevant VS Code modeline options before introducing Vim modelines
** Decision: also add Emacs modelines (`# -*- mode: sh; ... -*-`) alongside Vim modelines
* [x] Add Vim and Emacs modelines to applicable files
** Canonical three-line header block added to all scripts that had `# code:` modelines:
+
[source,bash]
----
# code: language=bash insertSpaces=true tabSize=2
# -*- mode: sh; sh-shell: bash; indent-tabs-mode: nil; tab-width: 2 -*-
# vim: ft=bash:et:ts=2:sts=2:sw=2
----
** `fix-style.sh`, `bin/git-fix-chmod`, `bin/xsuspend` also updated (were missing modelines entirely)
** All scripts moved from `Useful-Scripts/` to `bin/` and `system-scripts/` verified to have correct modelines
** `fix-modelines.py` added to repo root to normalize ordering/deduplication going forward
* [x] Align Vim modelines with existing VS Code modelines where both are present
** `filetype` matches `language` (bash/zsh); indent settings match `insertSpaces`/`tabSize`
* [x] Add/verify references for modeline setup details:
** https://vim.fandom.com/wiki/Modeline_magic[Vim Wiki: Modeline magic]
** NOTE: links to vimdoc from the wiki appear to be broken
** Working references:
*** https://vimdoc.sourceforge.net/htmldoc/usr_21.html#21.6[Vim User Manual 21.6]
*** https://vimdoc.sourceforge.net/htmldoc/starting.html#vimrc[Vim help: vimrc]


[[archived-scripts-review]]
== A.2 Archived Scripts Review

Scripts moved to `_archive/` during Useful-Scripts integration. Each needs individual
evaluation: keep as-is, update/rewrite, find an alternative solution, or delete.

* [ ] `_archive/audio-volume.sh` — Toggle mute and change volume via hotkeys.
  Deprecated; superseded by `bin/media-keys`. Confirm full overlap then delete.
* [ ] `_archive/gallery.sh` — Create a simple static HTML image gallery with JPEG
  thumbnails. Very old (2005); depends on `libjpeg-progs`. Evaluate if still useful
  or whether a modern alternative (e.g. sigal, thumbsup) is preferred.
* [ ] `_archive/getbyext.sh` — Fetch media files from a URL by extension using wget.
  Very old (2001). Compare functionality with `bin/getbyext`; delete if redundant.
* [ ] `_archive/pdfprint.sh` — Print PDF/PS files with configurable n-up and duplex
  via `psnup`. Old (2003); check if still functional and whether cups/lp alternatives
  suffice.
* [ ] `_archive/backup2ftp.sh` — Copy local backups to an FTP server. Old (2010),
  uses legacy FTP. Consider replacing with rsync/sftp/rclone if FTP backup is still
  needed.
* [ ] `_archive/wget-mp.py` — Parallel file fetching using wget. Old (2010), Python 2
  only. Evaluate against `bin/wget-p` and modern alternatives (aria2c, curl-based).
* [ ] `_archive/clear-cache.sh` — Clear known local caches and temp files. Old (2011);
  cache paths may be stale. Review and update paths if still needed, else delete.
