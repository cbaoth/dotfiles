#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash; indent-tabs-mode: nil; tab-width: 2 -*-
# vim: ft=bash:et:ts=2:sts=2:sw=2
# code: language=bash insertSpaces=true tabSize=2
# shellcheck shell=bash
#
# Suspend the system after optional lock-screen and audio cleanup steps.
## lock screen if not in a light-dm session
#ps aux | awk '$12=="light-dm" {exit 0} END{exit 1}' \
#  ||
# i3lock
#dm-tool lock

# kill pulseaudio (doesn't always recover, respawn instead)
pkill -9 pulseaudio

# suspend system
systemctl suspend
